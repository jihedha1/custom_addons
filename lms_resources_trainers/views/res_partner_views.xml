<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Extension du partenaire pour le marquer comme formateur -->
        <record id="view_res_partner_form_inherit_resources" model="ir.ui.view">
            <field name="name">res.partner.form.inherit.resources</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='category_id']/../.." position="after">
                    <field name="is_trainer" string="Est formateur"/>
                </xpath>

                <xpath expr="//page[@name='internal_notes']" position="after">
                    <page string="Formateur" attrs="{'invisible': [('is_trainer', '=', False)]}">
                        <button name="action_view_trainer_profile"
                                type="object"
                                class="oe_highlight"
                                string="Voir le profil formateur"
                                icon="fa-user"/>
                    </page>
                </xpath>
            </field>
        </record>
    </data>
</odoo>